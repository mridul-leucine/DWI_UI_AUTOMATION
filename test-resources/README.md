# Test Resources

This directory contains test assets used during automated testing.

## Structure

- `images/` - Sample images for file upload testing
  - Add `sample-test-image.jpg` or any test image file here

## Usage

Test images are referenced in test data files and used for parameter types that require file uploads.

**Note**: Before running tests, ensure you have a valid test image file in the `images/` directory.
